#!/bin/zsh
cd "/Users/aflatoon/Desktop/untitled folder/GOAL SETTING AND PLANNING " || exit 1
echo "Starting Goal Setting app..."
echo "Please keep this Terminal window open while using the app."
echo ""
(sleep 4; open "http://localhost:3000") &
npm run dev
